change: faf-platform
created: 2026-08-15
preflight:
  pace: interactive
  artifact_store: hybrid
  delivery_strategy: ask-on-risk
  review_budget_lines: 800
dag:
  explore: done
  proposal: done
  specs: done
  design: done
  tasks: done
  apply: done (PR1, PR2, PR3 all implemented and committed)
  verify: done (final full re-verify PASS WITH WARNINGS — 0 CRITICAL, 0 WARNING remaining; see verify-report-final.md) — ready for archive
  archive: pending
next_recommended: sdd-archive
pr1:
  branch: faf-platform/pr1-core-algebra
  status: committed (a4e31ac docs, f3c9c6c feat), independently verified (31/31 tests, tsc clean)
  tasks_done: [0.1, 0.2, 1.1-1.7, 2.1-2.3]
  tasks_deferred: [0.3 to PR2]
  not_pushed: true
  deep_verify: "verify-report-pr1.md — PASS WITH WARNINGS, 0 CRITICAL, 3 WARNING (2 closed via 6b36631: oplus doc comment, trace-payload test; 1 inherited TDD-evidence-table gap closed separately via tdd-evidence.md), 1 SUGGESTION (closed via 6b36631: ThesisState.score doc comment)"
pr2:
  branch: faf-platform/pr2-ingestion
  status: committed (ac78520 feat, ec9e210 fix D5), independently verified (106/106 tests, tsc clean)
  tasks_done: [0.3, 3.1-3.12, 4.1-4.4, 5.1-5.2]
  size_exception: accepted (~1800-2500 lines forecast)
  not_pushed: true
  deep_verify: "verify-report-pr2.md — PASS WITH WARNINGS, 0 CRITICAL, 1 WARNING (RSI Wilder-smoothing bug, closed via D6/d102b0c), 2 SUGGESTIONS (no action required)"
pr3:
  branch: faf-platform/pr3-delivery
  status: committed (9f55b71 feat cycle+API, 5f80800 feat UI+e2e+n8n), independently verified (123/123 vitest, 1/1 playwright, tsc clean)
  tasks_done: [6.1-6.7, 7.1-7.3]
  not_pushed: true
  note: "context compaction occurred mid-PR3; orchestrator reconstructed state from disk, found and fixed a real tsc error (mistyped vitest mock) that had not been caught by the passing vitest suite, before resuming and completing the batch"
  post_verify_fixes: "d4ddbfc closed 3 of 4 WARNING + the 1 SUGGESTION from the original whole-change verify-report.md (Vercel cache doc, n8n Set-Symbol nodes, e2e multi-asset test, PRD pointers); the 4th WARNING (no formal TDD Cycle Evidence table) was left open by d4ddbfc despite its commit message claiming full closure — closed separately by tdd-evidence.md, see final_verify below"
deviations:
  D5: {status: approved, summary: "MACD RSP-QL window 26->50 candles; indicator periods 12/26/9 unchanged", reason: "omega=26 degenerates EMA(slow) to 1 point, histogram/sigma_H always 0, macd evidence permanently silent"}
  D6: {status: approved, summary: "RSI RSP-QL window 14->20 candles; indicator's own period (14) unchanged, passed explicitly to computeRSI", reason: "omega=14 makes the Wilder continuation loop never execute (13<13 false), only the seed simple-average ever fires; 20 (not 50, matching MACD/SMA) chosen deliberately to avoid a structural rho-collision that would force ominus's losing-thesis rho to always be exactly 0"}
delivery:
  strategy: ask-on-risk
  chain_strategy: stacked-to-main
  pr_split: 3 (PR1 core-algebra L3+L4, PR2 ingestion L1+L2+adapter [size:exception, ~1800-2500 lines], PR3 delivery routes+UI)
signoff: {D1: approved, D2: approved, D3: approved (defer to v2), D4: approved, D5: approved, D6: approved}
final_verify:
  report: "verify-report-final.md — PASS WITH WARNINGS, 0 CRITICAL, 1 WARNING found (not from D5/D6/RSI — a closure-bookkeeping gap: d4ddbfc and 6b36631 both claimed to close 'all N warnings' but each silently left the TDD-evidence-table finding open), 0 new SUGGESTION"
  closure: "openspec/changes/faf-platform/tdd-evidence.md added — formal RED/GREEN/TRIANGULATE/SAFETY-NET table for all 41 tasks, closing the finding for real this time (it had previously only ever existed inside a since-summarized Engram revision, not as a durable repo artifact)"
  status: "all findings from verify-report.md, verify-report-pr1.md, verify-report-pr2.md, and verify-report-final.md now closed — 0 CRITICAL, 0 open WARNING, 0 open SUGGESTION"
params:
  candle_timeframe: 1h
  v1_assets: [BTCUSDT, ETHUSDT, SOLUSDT]
  note: "1h candles mean most 1-5min n8n cycles recompute against the same in-progress candle; sdd-tasks should address cron cadence alignment or document as expected"
